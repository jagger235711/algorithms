#include<iostream>
#include<cstring>
using namespace std;
char Num[1000];
int tmpNum[1000];
int m;
int V(int i, int j) {
	if(m==0)
		V(i,j)=
}
int main()
{
	while (cin >> m>>Num) {
		int len = strlen(Num);
		V(m, len);
	}
	return 0;
}